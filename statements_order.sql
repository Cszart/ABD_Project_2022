/*
    Estos comandos SQL deberian de ser ejecutados de manera secuencial
    para mantener el codigo organizado se ha colocado algunos comandos
    en otros archivos, sin embargo se coloco el nombre del archivo a cual
    se debe referir.

    Por ejemplo, lo primero que se debe hacer es borrar (en caso de que existan)
    la informacion de la base de datos, esos comandos estan en drop_tables.sql
*/

-- Create a new user in DB -- create_user.sql

-- Delete tables if exist -- drop_tables.sql
 
-- Create tables -- create_tables.sql

-- Insert data to test constraints -- insert_tables.sql

-- Create Roles -- create_roles.sql